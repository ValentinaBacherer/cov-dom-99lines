const friends = ['Joe', 'Clara', 'Ana', 'Ella', 'Andrea']

for (friend of friends){
    console.log(`${friend}`);
    for(let i=0 ; i<100 ; i++)
console.log(`${99-i}lines of code in the file,${99-i}lines of code; ${friend} strikes one out`);
}
